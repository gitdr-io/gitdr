apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gitdr.fullname" . }}-config
  labels:
    {{- include "gitdr.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
