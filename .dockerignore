# The build context is Go sources and nothing else. Everything dot-prefixed (vcs, editor
# and tool state), every build output, and anything key-shaped stays out of the context so
# it can never end up in an image layer.
.*
bin
dist
coverage.out
*.pem
*.key
*.bundle
*.prom
